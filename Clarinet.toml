[project]
name = 'decentralized-insurance-protocol'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.claims-processing-engine]
path = 'contracts/claims-processing-engine.clar'
clarity_version = 2
epoch = 2.5

[contracts.insurance-pool-manager]
path = 'contracts/insurance-pool-manager.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
